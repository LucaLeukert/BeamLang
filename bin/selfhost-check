#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
project_root="$(cd "${script_dir}/.." && pwd)"

(
  cd "${project_root}"
  mix beamlang run selfhost/test/cli_parse_test.bl
  mix beamlang run selfhost/test/lexer_test.bl
)
