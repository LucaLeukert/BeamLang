fn always_one(value: any) -> number {
    return 1;
}

fn main(args: [String]) -> number {
    let chars = "hi"->chars();
    let first = chars->first();
    let mapped = first->map(always_one);
    return match (mapped) {
        case?some value => value,
        case?none => 0
    };
}
