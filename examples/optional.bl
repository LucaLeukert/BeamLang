fn find_name(id: number) -> String? {
    if (id == 1) {
        return?some "Peter";
    } else {
        return?none;
    }
}

fn main() -> number {
    let name = find_name(1);
    let un = name->unwrap("Unknown");
    println(un);

    match (find_name(1)) {
        case?some name => {
            println(name);
            return 0;
        }
        case?none => {
            println("not found");
            return 1;
        }
    }
}
