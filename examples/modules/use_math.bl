fn main(args: [String]) -> number {
    let test: math_ops::MathPair = {
        left = 5,
        right = 10
    };
    println("The sum of ${test->left} and ${test->right}");
    return math_ops::add(test->left, test->right);
}
