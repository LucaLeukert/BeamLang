fn main(args: [String]) -> number {
    let mut count = 0;

    while (true) {
        count = 1;
        break;
    }

    loop {
        break;
    }

    let none = ?none;
    println(none->is_present());

    for (ch in "hi"->chars()) {
        println(ch);
    }

    return count;
}

fn test(self: String) -> void {
    return;
}