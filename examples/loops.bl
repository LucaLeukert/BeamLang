fn main() -> number {
    let mut count = 0;

    while (true) {
        count = 1;
        break;
    }

    loop {
        break;
    }

    for (ch in "hi") {
        println(typeof(ch));
    }

    return count;
}
