export error LexError {
    message: String,
    line: number,
    col: number
}

export fn lex_error(message: String, line: number, col: number) -> LexError {
    let err: LexError = {
        message = message,
        line = line,
        col = col
    };
    return err;
}
